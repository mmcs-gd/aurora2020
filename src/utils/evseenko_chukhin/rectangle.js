export default class Rectangle
{
    corner_x;
    corner_y;
    size_x;
    size_y;

    constructor(corner_x, corner_y, size_x, size_y)
    {
        this.corner_x = corner_x;
        this.corner_y = corner_y;
        this.size_x = size_x;
        this.size_y = size_y;
    }
}